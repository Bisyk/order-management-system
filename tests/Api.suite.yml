# Codeception Test Suite Configuration
#
# Suite for API tests

actor: ApiTester
modules:
    enabled:
        - REST:
            url: http://localhost:8000
            depends: PhpBrowser
            part: Json
        - PhpBrowser:
            url: http://localhost:8000
        - \Tests\Support\Helper\Api
step_decorators: ~
